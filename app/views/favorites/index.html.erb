<div class="page-header">
  <h1>
    Favorites
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Fund</th>
        <th>Active Share</th>
        <th>View</th>
      </tr>

      <% @favorites.each do |favorite| %>
      <tr>
        <td><%= favorite.id %></td>
        <td><%= favorite.id %></td>
        <td>
          <a href="/favorites/<%= favorite.id %>" class="btn btn-warning">Show</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
